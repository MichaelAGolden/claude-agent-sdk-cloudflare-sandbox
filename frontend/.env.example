# Frontend Environment Variables
#
# Vite uses different .env files based on the command:
#   - npm run dev   -> loads .env (development)
#   - npm run build -> loads .env.production (production)
#
# Setup:
#   1. Copy this file to .env for local development
#   2. Create .env.production for production builds

# ============================================================
# .env (for local development with npm run dev)
# ============================================================
# Clerk Authentication - DEVELOPMENT KEY
# Get from: https://dashboard.clerk.com -> API Keys -> Development
# Development keys (pk_test_...) work on localhost
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your_development_key_here

# ============================================================
# .env.production (for production builds with npm run build)
# ============================================================
# Create a separate .env.production file with:
# VITE_CLERK_PUBLISHABLE_KEY=pk_live_your_production_key_here
#
# Production keys (pk_live_...) are domain-locked and won't work on localhost
